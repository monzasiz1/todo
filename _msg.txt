Groups Phase 5: production polish

- Frontend GroupsPage: myPerms + can() helper berechnet die effektiven
  Berechtigungen des aktuellen Users (Owner/Admin = alle, sonst Custom-Rolle
  oder member_permissions, Fallback Defaults) - genau wie der Server.
- Eintrag-hinzufuegen Button respektiert create_tasks: disabled + Shield-Icon
  + Tooltip 'Fuer deine Rolle gesperrt', Toast wenn ohne Recht geklickt.
- Invite-Panel jetzt fuer Member mit invite-Recht sichtbar (vorher isAdmin only),
  Backend assertMemberCan('invite') war bereits aktiv.
- api/groups.js: invalidateGroupMembersCache() invalidiert nach
  PUT /permissions, POST/PUT/DELETE /roles fuer alle Mitglieder ueber
  invalidateCacheOnEvent(userId, 'group_change'). PATCH custom-role
  invalidiert nur den betroffenen User.
- Custom-Rollen Validierung: Rollennamen case-insensitive eindeutig pro
  Gruppe (POST + PUT), 400 bei Duplikat.